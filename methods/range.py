"""
range(): The range() function returns a list containing all the numbers within the
specified range. For example, range(5) will return a list [0, 1, 2, 3, 4]. This means
that range() generates the entire sequence in memory, which can be memory intensive for large ranges.

"""

for num in range(5):
    print(num)
